export interface Facility {
    facil_nm: string;          // 시설명
    res_telno: string;         // 대표자 전화번호
    road_addr: string;         // 도로명 주소
    faci_daddr: string;        // 시설 상세 주소
    main_event_nm: string;     // 주 종목명
    row_num: string;           // 순번
    city_cd: string;           // 시도 코드
    city_nm: string;           // 시도명
    local_cd: string;          // 시군구 코드
    local_nm: string;          // 시군구명
}
